[[source]]
url = "https://pypi.org/simple"
verify_ssl = true
name = "pypi"

[packages]

[dev-packages]
autopep8 = "*"
coverage = "*"
isort = "*"
pytest = "*"

[requires]
python_version = "3.8"

[scripts]
test = "coverage run --branch --omit=.venv/* -m pytest"
test-coverage = "coverage report"
